package main

import "fmt"

func main() {
	var i, j int = 1, 2
	k := 3
	a, b := "a", "b"
	var x, y = 'x', 'y'
	c, python, java := true, false, "no!"

	fmt.Println(i, j, k, c, python, java, a, b, x, y)
}
